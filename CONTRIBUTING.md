# Руководство по вкладу в проекты NotDoneByOne

Добро пожаловать в NotDoneByOne! Это руководство описывает процесс внесения вклада в наши проекты. Пожалуйста, ознакомьтесь с ним перед началом работы.

## 📋 Перед началом

1. **Прочтите [Onboarding Guide](https://github.com/NotDoneByOne/.github/blob/main/ONBOARDING.md)** — содержит основные правила работы и философию организации.
2. **Ознакомьтесь с [Code of Conduct](https://github.com/NotDoneByOne/.github/blob/main/CODE_OF_CONDUCT.md)** — правила поведения в сообществе.
3. **Убедитесь, что у вас есть доступ к необходимым инструментам** — Git, аккаунт GitHub, инструменты разработки для конкретного проекта.

## 🎯 Философия NotDoneByOne

Наши принципы работы:

1. **Для каждого проекта свое решение** — каждый проект и библиотека должны быть спроектированы, исходя из существующих ресурсов и конкретных условий решения задач.
2. **Код как форма коммуникации** — пишите код так, чтобы его мог понять другой разработчик. Читаемость и ясность важнее краткости.
3. **Рабочий результат важнее идеальной абстракции** — ценностью являются законченные, функционирующие блоки, которые можно протестировать и использовать.
4. **Коллективный разум** — задавать вопросы для прояснения задачи или реализации — нормальная практика.

## 🔄 Процесс вклада

### Для внутренних разработчиков (членов организации NDO)

#### 1. Начало работы
1. **Получите доступ** к организации GitHub и настройте двухфакторную аутентификацию (2FA).
2. **Представьтесь команде**, создав issue в репозитории `.github` с типом `chore` и заголовком `[Onboarding] Имя Фамилия`.
3. **Изучите существующие проекты**, прочитав их `README.md` и документацию.

#### 2. Жизненный цикл задачи
**Все изменения начинаются с создания Issue.**

1. **Создание Issue:**
   - Используйте соответствующие шаблоны (Bug Report, Feature Request)
   - Четко сформулируйте цель и контекст задачи
   - Для багов укажите шаги воспроизведения, ожидаемый и фактический результат
   - Назначьте соответствующие метки (см. раздел "Система меток")
   - Назначьте себя (`Assignees`), если берете задачу в работу

2. **Ветвление:**
   ```bash
   git checkout -b <version>/<issue_num>-<short-description>
   ```
   Примеры:
    - `1.0.0/123-auth-oauth`
    - `2.1.3/1234-api-null-response`

  **Примечание:** Ветка `main` защищена. Прямые пуши запрещены. Все изменения через Pull Request.

3. **Разработка:**
  - Соблюдайте соглашения о стиле кода проекта
  - Создавайте атомарные коммиты с использованием Conventional Commits
  - Обновляйте документацию при необходимости

4. **Создание Pull Request:**
  - Создайте Draft PR в начале работы
  - Заполните шаблон PR полностью
  - Ссылайтесь на связанную issue (например, Closes #123)
  - Добавьте описание изменений согласно общепринятому шаблону: что, почему и как
  - Для UI изменений приложите скриншоты
  - Запросите ревью у назначенных ревьюверов

### Для внешних контрибьюторов (Open-Source)

1. Первые шаги
  - Выберите проект, изучив репозитории организации и их README.md.
  - Прочтите документацию проекта, включая CONTRIBUTING.md и README.md.
  - Найдите задачу с метками первая задача или нужна помощь.

2. Процесс внесения изменений
  - Создайте форк целевого репозитория.
  - Клонируйте ваш форк: `git clone https://github.com/ваш-аккаунт/репозиторий.git`
  - Добавьте upstream: `git remote add upstream https://github.com/NotDoneByOne/репозиторий.git`
  - Создайте ветку с соответствующим именем (см. правила ветвления выше).
  - Внесите изменения, следуя стандартам кода проекта.
  - Актуализируйте ветку перед созданием PR:
    
    ```
    git checkout main
    git fetch upstream
    git merge upstream/main
    git checkout ваша-ветка
    git rebase main
    ```

  - Создайте Pull Request из вашего форка в соответствующую ветку оригинального репозитория.

## Система меток
Мы используем стандартизированную систему меток на русском языке:

### Типы задач
`type: ошибка` — отчет об ошибке или некорректном поведении
`type: функциональность` — предложение или реализация новой функциональности
`type: улучшение` — улучшение существующей функциональности
`type: документация` — задачи, связанные с документацией
`type: рефакторинг` — переработка кода без изменения функциональности
`type: технический долг` — рутинные задачи, обновление зависимостей

### Приоритеты
`priority: высокий` — требует немедленного внимания, блокирует работу
`priority: средний` — важно, но может быть отложено
`priority: низкий` — косметические улучшения, можно отложить

### Статусы
`status: заблокировано` — задача ожидает внешних факторов
`status: в работе` — задача находится в разработке
`status: требуется ревью` — код готов к проверке

### Специальные метки
`первая задача` — подходит для новых контрибьюторов
`нужна помощь` — требуется помощь сообщества
`срочно` — требует немедленного внимания
`критический баг` — критическая ошибка, блокирующая работу

## Стиль кода и коммитов
### Conventional Commits
Используйте следующий формат для сообщений коммитов:

`<type>(<scope>): <description>`

**Типы:**
`feat` — новая функциональность
`fix` — исправление ошибки
`docs` — изменения в документации
`style` — форматирование, отсутствие изменений в коде
`refactor` — рефакторинг кода
`test` — добавление или исправление тестов
`chore` — обновление инструментов, конфигураций

**Примеры:**  
  - `feat(auth): add OAuth 2.0 support`
  - `fix(api): handle null response in users endpoint`
  - `docs(readme): обновление инструкции по устновке`

### Правила именования веток

`<version>/<issue_number>-<short_description>`

**Примеры:**
  - `1.0.0/123-auth-oauth`
  - `2.1.3/45-fix-null-pointer`

## Код-ревью

**Для ревьюверов**
1. Отвечайте на запросы в течение 48 часов
2. Будьте конструктивны — комментируйте не только что не так, но и почему
3. Предлагайте альтернативные решения
4. Используйте стандартные действия GitHub: 
  - Approve (✅) — изменения приняты
  - Request Changes (✏️) — требуются доработки
  - Comment (💬) — общий комментарий

**Для авторов PR**
1. Отвечайте на каждый комментарий
2. Вносите изменения по замечаниям
3. Не воспринимайте критику лично — фокус на улучшении кода
4. Обновляйте PR после внесения изменений

**_Важно: Мерджить PR может только участник, не являющийся его автором._**

## Аварийные ситуации

### Критическая ошибка в ветке main
- Создайте issue с метками type: ошибка и priority: высокий
- Для блокирующих ошибок используйте метку критический баг
- Создайте ветку hotfix/ для немедленного исправления
- Оповестите команду в общем чате

### Конфликт в код-ревью
Если дискуссия зашла в тупик:
- Привлеките третьего участника в качестве арбитра
- Рассмотрите альтернативные подходы
- Сфокусируйтесь на технических аргументах

### Блокировка задачи
Если вы не можете продвинуться более одного рабочего дня:
- Создайте комментарий в Issue с описанием проблемы
- Используйте метку `status: заблокировано`
- Запросите помощь у команды

## Поддержка и вопросы

**Где задавать вопросы?**
- GitHub Issues — для технических вопросов по проекту
- Чат организации — для срочных вопросов и координации
- Личные сообщения — для приватных вопросов

**Сроки ответа**
- Для внутренних разработчиков: ответ в течение 48 часов
- Для внешних контрибьюторов: первичный ответ в течение 7 рабочих дней
- Если ответа нет — вежливо напомните в комментарии.

---

## 🤝 Благодарности
Спасибо за ваш вклад! Каждое улучшение, от исправления опечаток до реализации новых функций, помогает сделать наши проекты лучше.

---
NotDoneByOne, 2026

Это живой документ. Предложения по улучшению приветствуются через Issue или Pull Request в репозитории .github.
